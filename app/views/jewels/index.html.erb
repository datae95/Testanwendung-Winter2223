<h1>Jewels</h1>
<%= link_to "New jewel", new_jewel_path %>

<div id="jewels">
  <% @jewels.each do |jewel| %>
    <%= render jewel %>
    <p>
      <%= link_to "Show this jewel", jewel %>
    </p>
  <% end %>
</div>

